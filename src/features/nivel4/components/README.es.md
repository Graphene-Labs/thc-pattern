# components/

Sin cambios respecto al Nivel 1. Ver [`nivel1/components/`](../../nivel1/components/README.es.md).

El componente no sabe que el Composer ahora vive en `services/` ni que existe `composeConfig`.
